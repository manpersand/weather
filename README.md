## My first react app. A simple weather widget.

### A config.json that contains an api key from OpenWeather is required in the src folder.

### format for the config.json file:

{
"apiKey": "**_your key here_**"
}
